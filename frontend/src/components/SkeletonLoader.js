import { View, Text, Pressable } from 'react-native'
import React from 'react'


const SkeletonLoader = ({ isVisibleskeletion }) => {
    return (
        <Pressable></Pressable>
    )
}

export default SkeletonLoader